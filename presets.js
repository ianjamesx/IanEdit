/*
  presets for the system software course
  COSC 350
*/

var presets = {

  systemsoftware:
   `#include <stdio.h>
    #include <fcntl.h>
    #include <stdlib.h>
    #include <unistd.h>
    #include <sys/stat.h>

    int main(int argc, char** argv){

      return 0;
    }`

};

module.exports = presets;
